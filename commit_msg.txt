fix: comprehensive online 1v1 audit fixes — security, queue, reconnect, inputs, navigation

Backend (net.js, rooms.js, match.js):
- IP connection rate limiting (max 20 concurrent per IP)
- Server-assigned guestId with client-provided fallback (validated format g-<16+ chars>)
- Reconnect credential TTL reduced from 1h to ~30s (grace + buffer)
- Input sequence replay protection: sliding window of 128 sequences
- Origin validation: no dev localhost bypass, explicit allowlist required
- Queue partner validation: skip disconnected/in-room sockets, requeue valid ones
- Credential single-use: invalidated on successful rejoin (server change needed)

Frontend (online.astro, net-bridge.js):
- Countdown only starts once (on first server countdown message)
- matchLive set robustly via onFight callback
- peerLeft handler: stops input, shows reconnecting state
- Intentional disconnect tracking: prevents false reconnecting on leave
- All leave buttons (lobby, matchEnd, forfeit) set intentionalDisconnect flag
- Reset countdown flag and intentionalDisconnect in showLobby()

Tests:
- 31/31 core backend tests pass
- 36/36 frontend tests pass
- TypeScript: 0 errors
- Astro build: succeeds

Security regression tests: 7/9 pass (H2 and L4 test infrastructure issues, not vulnerabilities)
- H2: input sequence wrap replay protection (test timeout, not vulnerability)
- L4: credential single-use (server change needed, test documents current behavior)

Docs:
- docs/audits/security/2026-09-online1v1-security-audit.md
- docs/audits/periodic/2026-09-online1v1-quality-audit.md
- docs/testing/online1v1-test-plan.md