{
  "name": "nox",
  "version": "0.1.0",
  "private": true,
  "description": "Nox (NEON VOID) — 2-player arena duel. Monorepo: Astro frontend (Vercel) + Node backend (Render).",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "check": "npm --prefix frontend run check",
    "preview": "npm --prefix frontend run preview",
    "start": "npm run build && npm --prefix backend start",
    "test": "npm run build && npm --prefix backend test"
  }
}
